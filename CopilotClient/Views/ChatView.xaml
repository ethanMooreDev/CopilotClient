<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="CopilotClient.Views.ChatView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CopilotClient.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:selectors="using:CopilotClient.Selectors"
>
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="ms-appx:///Resources/MessageTemplates.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>

    </UserControl.Resources>

    <Grid x:Name="RootGrid" Background="#0B1120" Padding="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Messages list -->
        <ScrollViewer Grid.Row="0" x:Name="MessageScrollViewer">
            <ItemsControl
                x:Name="MessagesItemsControl"
                ItemTemplateSelector="{StaticResource MessageTemplateSelector}"
                ItemsSource="{Binding Messages}"
                Width="800"
            >
                <ItemsControl.ItemTemplate/>
            </ItemsControl>
        </ScrollViewer>

        <!-- Input row -->
        <Grid Grid.Row="2" Margin="0,12,0,0" Width="800">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBox 
                Grid.Column="0"
                Text="{Binding InputText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                AcceptsReturn="False"
                TextWrapping="Wrap"
                MinHeight="40"
                MaxHeight="100"
                PlaceholderText="Ask something..."
                Margin="0,0,8,0"
                KeyDown="TextBox_KeyDown"
            />

            <Button 
                Grid.Column="1"
                Content="Send"
                IsEnabled="{Binding SendEnabled, Mode=OneWay}"
                Command="{Binding SendCommand}"
                VerticalAlignment="Bottom"
                Padding="16,8"
            />
        </Grid>
    </Grid>
</UserControl>
